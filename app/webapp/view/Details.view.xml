<mvc:View
    controllerName="com.moyo.demo.caplugins.controller.Details"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:l="sap.ui.layout"
    xmlns:f="sap.f"
    xmlns:uxap="sap.uxap"
>
    <uxap:ObjectPageLayout
        id="objectPageLayout"
        showTitleInHeaderContent="true"
        alwaysShowContentHeader="false"
        preserveHeaderStateOnScroll="false"
        headerDesign="Light"
        isChildPage="true"
    >
        <uxap:headerTitle>
            <uxap:ObjectPageHeader
                objectTitle="{brand} {model}"
                objectSubtitle="{year} - {fuelType}"
                isObjectIconAlwaysVisible="false"
                isObjectTitleAlwaysVisible="true"
                isObjectSubtitleAlwaysVisible="true"
                showPlaceholder="true"
                showNavButton="true"
                navButtonPress=".onNavBack"
            >
                <uxap:navigationBar>
                    <!-- Prominent back button so it's always clearly visible -->
                    <Bar>
                        <contentLeft>
                            <Button icon="sap-icon://nav-back" text="Back to Vehicles" press="onNavBack" type="Default" />
                        </contentLeft>
                        <contentMiddle>
                            <Title text="Moyotech" />
                        </contentMiddle>
                        <contentRight>
                            <Button icon="sap-icon://phone" text="+996 247-1680" type="Transparent" />
                        </contentRight>
                    </Bar>
                </uxap:navigationBar>
                <uxap:actions>
                    <Button
                        text="{= ${status} === 'Available' ? 'Rent this vehicle' : 'Already Rented'}"
                        type="{= ${status} === 'Available' ? 'Emphasized' : 'Default'}"
                        enabled="{= ${status} === 'Available'}"
                        press=".onRentPress"
                    />
                </uxap:actions>
            </uxap:ObjectPageHeader>
        </uxap:headerTitle>

        <uxap:headerContent>
            <FlexBox wrap="Wrap" alignItems="Start">
                <VBox class="sapUiSmallMarginEnd">
                    <Label text="Price" />
                    <ObjectNumber
                        number="{pricePerDay}"
                        numberUnit="$ / day"
                        emphasized="true"
                    />
                </VBox>
                <VBox class="sapUiSmallMarginEnd">
                    <Label text="Status" />
                    <ObjectStatus
                        text="{status}"
                        state="{= ${status} === 'Available' ? 'Success' : 'Error' }"
                        inverted="true"
                    />
                </VBox>
                <VBox>
                    <Label text="Seats" />
                    <Text text="{seats}" />
                </VBox>
            </FlexBox>
        </uxap:headerContent>

        <uxap:sections>
            <uxap:ObjectPageSection title="Overview">
                <uxap:subSections>
                    <uxap:ObjectPageSubSection>
                        <VBox>
                            <Image src="{imageURL}" width="100%" height="400px" mode="Background" backgroundSize="contain" />
                            <Title text="Description" level="H3" class="sapUiMediumMarginTopBottom" />
                            <Text text="{description}" />
                        </VBox>
                    </uxap:ObjectPageSubSection>
                </uxap:subSections>
            </uxap:ObjectPageSection>

            <uxap:ObjectPageSection title="Technical Details">
                <uxap:subSections>
                    <uxap:ObjectPageSubSection>
                        <l:Grid containerQuery="true" defaultSpan="XL3 L4 M6 S12">
                            <GenericTile header="Max Speed" subheader="{maxSpeed} km/h" press=".onTilePress">
                                <TileContent>
                                    <core:Icon src="sap-icon://speed" size="2rem" color="Default" />
                                </TileContent>
                            </GenericTile>
                            <GenericTile header="Engine Type" subheader="{engineType}" press=".onTilePress">
                                <TileContent>
                                    <core:Icon src="sap-icon://settings" size="2rem" color="Default" />
                                </TileContent>
                            </GenericTile>
                            <GenericTile header="Fuel Consumption" subheader="{fuelConsumption} L/100km" press=".onTilePress">
                                <TileContent>
                                    <core:Icon src="sap-icon://pipeline-medicine" size="2rem" color="Default" />
                                </TileContent>
                            </GenericTile>
                        </l:Grid>
                    </uxap:ObjectPageSubSection>
                </uxap:subSections>
            </uxap:ObjectPageSection>
        </uxap:sections>
    </uxap:ObjectPageLayout>
</mvc:View>